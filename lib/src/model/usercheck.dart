class UserCheck {
  String username;
  String name;
  String phone;
  String birth;
  int gender;
  String token;
  bool isFirstLogin;

  UserCheck({this.username, this.name, this.phone, this.birth, this.gender, this.token, this.isFirstLogin});
}
